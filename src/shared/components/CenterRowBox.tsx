'use client';
import {Box, styled} from '@mui/material';

const CenterRowBox = styled(Box)(({theme}) => ({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
}));

export default CenterRowBox;
